<h2 class="text-2xl font-bold mb-6">Benutzerprofil</h2>

<div class="bg-white border border-gray-300 rounded-md shadow p-6">
  <p><strong>Email:</strong> <%= @user.email %></p>
  <p><strong>Erstellt am:</strong> <%= @user.created_at.strftime("%d.%m.%Y %H:%M") %></p>
  <p><strong>Aktualisiert am:</strong> <%= @user.updated_at.strftime("%d.%m.%Y %H:%M") %></p>
</div>

<%= link_to "Zurück zur Übersicht", users_path, class: "text-blue-600 hover:underline mt-4 block" %>
