<div class="max-w-lg bg-white shadow rounded-lg p-6 mb-6">
  <h2 class="text-xl font-semibold mb-4"><%= server.name %></h2>
  <p class="text-gray-600 mb-2">Status: <%= server.active? ? "Aktiv" : "Inaktiv" %></p>
  <p class="text-gray-600 mb-4">Zuletzt bearbeitet: <%= server.updated_at.strftime("%d.%m.%Y %H:%M") %></p>

  <div class="flex flex-col space-y-2">
    <%= link_to " Informationen", server_path(server), class: "text-blue-600 hover:underline" %>
    <%= link_to " Bearbeiten", edit_server_path(server), class: "text-green-600 hover:underline" %>
    <%= link_to " Löschen", server, method: :delete, data: { confirm: "Bist du sicher?" }, class: "text-red-600 hover:underline" %>
  </div>
</div>

