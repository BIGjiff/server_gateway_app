<div class="max-w-lg mx-auto bg-white border border-gray-200 rounded-xl shadow-md p-6">
  <h2 class="text-xl font-semibold mb-4">Server-Details</h2>

  <div class="space-y-3 text-sm text-gray-700">
    <p><strong>Name:</strong> <%= @server.name %></p>
    <p><strong>Beschreibung:</strong> <%= @server.description.presence || "Keine Beschreibung" %></p>
    <p><strong>Gateways:</strong> <%= pluralize(@server.gateways.count, "Gateway") %></p>
  </div>

  <div class="flex justify-end gap-3 mt-6">
    <%= link_to "Bearbeiten", edit_server_path(@server),
                class: "px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition" %>
    <%= link_to "Zurück", servers_path,
                class: "px-4 py-2 bg-gray-200 text-gray-800 rounded-lg hover:bg-gray-300 transition" %>
  </div>
</div>
